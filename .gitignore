# Nuxt
.nuxt
.output
.data
.nitro
.cache
dist

# Tooling artifacts
contrast-report.json

# Node
node_modules
*.log
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.idea
.vscode

# AI tooling — local state + agent-facing docs (kept out of the
# repo so the canonical conventions live in design/ + docs/adr/,
# not in AI-only artifacts that drift)
.claude/
CLAUDE.md
.cursor/
.cursorrules
.aider*
.windsurfrules
.continue/

# Raw GeoJSON sources are large (13MB+) and re-fetchable from
# their official endpoints. The build-geo script projects +
# simplifies them into the small TS modules under
# app/assets/geo/. We ship the projected outputs, not the raw.
data/source/
